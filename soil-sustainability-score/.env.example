# Copy this file to .env and fill in real values. Never commit .env itself.

FLASK_ENV=development
FLASK_APP=src/api/app.py
SECRET_KEY=change-me

# PostgreSQL + PostGIS
DATABASE_URL=postgresql://username:password@localhost:5432/soil_sustainability

# Optional: weather API key if you use live weather data
WEATHER_API_KEY=
